## HTML
* `GET /` StaticPagesController#root
## API Endpoints
### `users`
* `POST /api/users` - user sign up
### `session`
* `POST api/session` - login
### `boards`
* `GET api/boards` - returns all boards
* `GET api/boards/:id` - returns a specific board
* `POST api/boards/` - persists new board to back-end database
* `PATCH api/boards/:id' - updates a board and persists new data to back-end database

### `lists`
* `GET api/lists/:id` - returns a specific list
* `POST api/lists/` - persists new post to back-end database
* `PATCH api/lists/:id' - updates a list and persists new data to back-end database


### `cards`
* `GET api/cards/:id` - returns a specific card
* `POST api/lists/` - persist new card to back-end database
* `PATCH api/cards/:id' - updates a list and persists new data to back-end database

